# VerGE (Version Greater Than or Equal m-function)

Matlab function that will determine if a cell array of values is greathan or equal to a reference version.


## Tests

To run, use the command `runtests`.